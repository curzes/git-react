import React from 'react';

export default function SearchOrJumpTo() {
    return (
        <form>
            <label
                placeholder="search or jump to..."
            >
                <input
                    type="text"
                />
            </label>
        </form>
    );
}
